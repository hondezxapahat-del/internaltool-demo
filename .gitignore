.env
__pycache__/
*.pyc
.venv/
venv/
node_modules/
*.log

# Real third-party source material kept locally for reference only —
# never part of the RAG ingestion pipeline, must never be committed.
docs/*.pdf
